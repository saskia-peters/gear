---
topic: G.E.A.R.
goal: Proceed to Epics & Stories from finalized Architecture
updated: 2026-08-30T12:52
---

- (event) Started product brief process for G.E.A.R.
- (decision by user) User selected Coaching Path
- (note by user) Target platform: responsive web app (mobile-friendly)
- (note by user) Core requirements: central user repo, user groups, permission groups, email login
- (note by user) First module: Tool maintenance/inspection schedule
- (note by user) Tool inspection relies on tool types, checklists, custom or default schedules, and user qualifications
- (note by user) Dashboard list status: Red (now), Orange (next 2 weeks), Green (OK)
- (decision by user) Standalone system, no integration with external THW tools in V1
- (decision by user) Self-registration is allowed but requires admin confirmation before account is active
- (requirement by user) Tool inspections can be simple pass/fail OR checklist-based (storing all item results), defined by tool type
- (decision by user) Initial permission groups: Helfer*in, Fuehrung, Admin
- (decision by user) Non-goals for V1: Tool reservations/booking, repair tracking/logs
- (event by coach) Drafted initial product brief at artifact path /home/saskia/.gemini/antigravity-cli/brain/690e5db4-ec0e-4cd2-8a82-655e3a3730fb/product_brief.md
- (decision by user) Failed inspection flags the tool as 'Out of Service' (status attribute)
- (decision by user) No automated notifications in V1 (deferred to future feature list)
- (decision by user) Data population via CSV import and manual additions in the Admin panel
- (event by coach) Updated product brief artifact with resolved open questions
- (event by coach) Finalized the product brief for G.E.A.R.
- (requirement by user) Required GDPR/DSGVO compliance
- (requirement by user) Required flexible attributes for Users, Tools, and Tool Types
- (requirement by user) Required Admin module isolation as a separate module in the ecosystem
- (requirement by user) Required optional MFA (TOTP) support during login
- (requirement by user) Required database persistence, Docker containerization, schema versioning (migrations), and configurable backups/restore
- (event by coach) Updated product brief in both workspace and artifacts folder to incorporate GDPR compliance, flexible attributes, admin isolation, MFA, DB persistence, containerization, backups/restore, and schema migrations
- (requirement by user) Required professional software engineering standards: maintainable, extendable, testable, and modularized codebase
- (event by coach) Updated product brief in both workspace and artifacts folder to incorporate software engineering standards (modular, maintainable, extendable, testable)
- (event) Finalized Architecture spine (feature altitude) at _bmad-output/planning-artifacts/architecture/architecture-app-ovsingen-2026-08-30/ARCHITECTURE-SPINE.md. Key decisions: modular monolith + hexagonal per module (User Directory & Auth, Tool Maintenance, Admin); PostgreSQL with first-class columns + JSONB extension surface; chi + sqlc/pgx + golang-migrate; React + Vite + TS SPA; auth owned by User module exposed as a shared port; tool status derived never stored; single shared per-tool inspection clock. 11 ADs incl. cross-module DSGVO/user-lifecycle flows, tool-config single write owner, schema/reference ownership. Reviewer gate applied (clean). Next: bmad-create-epics-and-stories.
- (decision by user) Ops/deploy tooling applied to spine: `just` (justfile) as single command interface; OpenTofu (v1.12.6) for cloud IaC (`infra/`); Google Cloud Run as testing/staging platform scaled to zero (min_instance_count=0). Production remains self-hosted single-host compose. Lint re-run clean.
- (decision by user) Spine made DB-precise + reviewable: Database Artifacts table (18 tables, owner-module), USER_GROUPS vs PERMISSION_GROUPS split, and 4 mermaid diagrams for non-technical readers (all validated). Then permission-model redesign via AD-12: action-matched additive RBAC, German role names `helfende`/`fuehrende`/`admin` + new 4th base role `schirrmeister` (equipment caretaker, full tool+tool-type mgmt), users multi-role, additive no-deny, permission↔action match, no groups-in-groups V1, admins create/name/assign groups and may edit base roles; new users seeded `helfende`. Added visible 17-code base permission series + role matrix. Lint + all 7 mermaid blocks validated clean. Next: bmad-create-epics-and-stories.
- (change) Rechecked user journeys (no stories exist yet; still in PRD stage). Added persona **Torsten (Schirrmeister)** to PRD JTBD + Glossary, and **UJ-4** exercising the schirrmeister permissions (`tools.manage`, `tool_types.manage`, `inspection.history.view`). Brief personas updated for consistency. Consistent with architecture AD-12 role matrix.
- (event) Set up Docusaurus docs site (NFR-M4 delivered early): site in `docs/` (Docusaurus 3.10.2 TS template), rendering the BMad planning artifacts (PRD, product brief, architecture spine, addendum) as browsable pages with `@docusaurus/theme-mermaid` diagram support. Added root `justfile` with docs recipes (install/start/build/serve/clear). Added `.github/workflows/docs.yml` building + deploying to GitHub Pages via `actions/deploy-pages` (no gh-pages branch; Pages `build_type: workflow`). Pages configured to deploy on all branches (environment `github-pages` policy null). Live at https://saskia-peters.github.io/gear/ — build + deploy verified green, all pages 200.
- (event) GEAR brand theming + Management Overview page: themed the Docusaurus site to GEAR brand — primary `#003399` (RAL 5002 GEAR-Blau) with navy/light-blue/white palette, orange `#f5821f` accent (also used in app status diagrams), GEAR monogram logo replaced the Docusaurus mascot. Applied via `docs/src/css/custom.css` (light+dark Infima overrides, navbar/footer/hero) and mermaid node colors. Added `docs/docs/management-overview.md` — a non-technical, diagram-rich overview for stakeholders (purpose, roles, inspection lifecycle flow, status traffic-light state diagram, benefits, V1 scope). Wired into navbar ("Overview"), sidebar (position 2), and intro. Favicon switched to SVG logo. Build + Pages deploy verified green; overview live at /docs/management-overview.
- (event) Self-service password management + dual-admin recovery (User module): added **FR-25** (change own password while logged in — confirm current pw, ≥10 chars, revoke other sessions), **FR-26** (forgot-password self-service reset via single-use 30-min emailed token/link; transactional account-recovery email only, explicitly NOT reopening the automated-notification non-goal §5/§6.2), and **FR-27** (dual-admin bootstrap — two pre-seeded admin accounts; an admin can only recover credentials with the other admin's approval; last-admin recovery is out-of-band manual w/ audit). Architecture: new **AD-13** (Self-Service Password Management + Dual-Admin Recovery) extending AD-2; new DB table **19 `password_reset_tokens`** (User module); new base permission **`admin.recovery.approve`** (admin-only role matrix); ER diagram updated; addendum "Password Management Decisions" (Argon2id, SMTP transactional sender, token hashing, dual-admin bootstrap). Sync'd planning copies into docs/docs/planning/ and updated management-overview ("passwords are yours to manage" + dual-control). ER diagram + docs build validated green.
- (event) SMTP delivery admin-configurable (FR-28): administrators can now configure SMTP/email-delivery parameters in the admin UI (host, port, security none/STARTTLS/TLS, sender/display name, username, password) and send a test email — no redeploy needed. Added **FR-28 (SMTP Configuration — Admin Interface)** to Epic 3 in the PRD. Architecture: new **AD-14 (Admin-Configurable Email/SMTP Settings, Single Writer)**; new DB table **20 `smtp_settings`** (owned by User module, configured via Admin UI through User config port — matches AD-10; Admin still owns no tables); new base permission **`admin.settings.email`** (admin-only); SMTP password stored **encrypted at rest** (key from env/secret-manager, NFR-S4) and write-only/masked in UI; ER diagram + role matrix + capability map + deferred section updated; addendum "Password Management Decisions" reconciled (runtime-configurable, no longer env-locked). Synced planning copies + management-overview; ER diagram + docs build verified green.
- (event) Admin module owns operational settings (SMTP + backup destinations): on product-owner direction, **SMTP settings moved out of the User module** into the **Admin module** (single system-wide config, configured once) — Admin's `smtp_settings` table (AD-14), with the User module's email adapter consuming them via Admin's settings port. Extended the same principle to **backup destinations**: new **FR-29 (Backup Destination Configuration)** — Admin configures ≥1 destination (S3-compatible, FTP/SFTP, local filesystem) with mechanism, endpoint, bucket/path, encrypted credentials, and a "test connection" action, replacing the earlier env-deferred stance; new Admin table **21 `backup_destinations`** + new **AD-15 (Admin-Configurable Backup Destinations, NFR-R3)**. New base permission **`admin.settings.backup`**; permission module columns for `admin.settings.email` corrected to Admin. Admin module now owns exactly two small config tables (smtp_settings, backup_destinations) — AD-11/AD-14/AD-15 updated; ER diagram gained an Admin (`a`) entity class; addendum "Operational Settings Decisions" added; PRD Epic 3 + admin description updated. Sync'd planning copies + management-overview; ER diagram + docs build verified green.
- (event) Named schedule catalog (FR-30, admin-owned): on product-owner direction, schedules become a **named catalog owned by the Admin module** (cross-cutting operational setting reusable by future modules — vehicle logs, event scheduling), consistent with SMTP/backup. New **FR-30 (Schedule Catalog Management — Admin Interface)**: admins create/edit/archive named schedules (e.g. "1 year", "1 quarter", "1 month", "2 weeks", "3 days"), each a name + repeating interval (unit/magnitude); rows are **prepared for future enhancement** with reserved cron-like **weekday-set** (one or several days) + **time-of-day** fields — nullable/unused in V1; a later composite engine activates them with no schema migration. Tool Types pick a default schedule and individual Tools may pick their own (FK → `schedules`); Tool module reads the catalog via Admin's schedule port (AD-11). Architecture: new **AD-16 (Named Schedule Catalog)**, new DB table **22 `schedules`** (Admin), AD-5/AD-9/AD-10/AD-11 updated (schedule FK replaces numeric `schedule_interval`/`schedule_override`), new base permission **`schedules.manage`** (admin-only), ER diagram gained **SCHEDULES** (Admin `a` class) with `default_schedule`/`chosen_schedule` relations, deferred section notes the composite weekday/time engine as future. PRD FR-8/FR-9 updated to reference the catalog; addendum "Operational Settings Decisions" gained schedule-ownership/shape/seed rows; Epic 3 desc + MVP scopes updated with an ordering note (schedule catalog lands before Epic 2 assignment). Sync'd planning copies + management-overview; ER + docs build verified green.
- (event) Interactive, expandable mermaid diagrams in the docs site (resolves the deferred "interactive diagram viewer" item): swizzled `@theme/Mermaid` in `docs/src/theme/Mermaid/` (index.tsx + styles.module.css, codedown). Every mermaid diagram now shows a hover expand button and is click-to-zoom; a fullscreen overlay opens with the diagram on a light panel over a dark backdrop, supporting drag-to-pan, scroll-wheel zoom (cursor-centered, clamped 0.5x–8x), and `+`/`−`/`Reset` toolbar, closing via `×`, backdrop, or `Esc`. `tsc` typecheck + full docs build both green; component verified present in the production JS bundle.
- (event) Renamed repo/dir app-ovsingen → gear + rebranded THW → GEAR: GitHub repo `saskia-peters/gear` (rename auto-redirects old URLs), local dir `~/devprojects/gear`, remote updated, docs baseUrl `/gear/`, commit `8f9acdc`. Dated review/artifact folder names (`prd-app-ovsingen-2026-08-29`, etc.) intentionally left as-is. App name displays as **G.E.A.R.** (site title/logo/docs); code identifiers/CSS vars/brand tokens (`GEAR-Blau`, `--gear-*`, comments) stay **GEAR** (no periods). External THW system names (THW Extranet, THW Helferportal, German Federal Agency...) kept.
- (reminder) MANUAL STEP pending after repo rename: GitHub Pages was reset by the rename — docs CI build passes but deploy fails with 404. Must re-enable Pages manually: repo Settings → Pages → Build and deployment → Source = **"GitHub Actions"** → Save, then re-run the `docs` workflow (or push). Site then live at https://saskia-peters.github.io/gear/ (old `.../app-ovsingen/` URL will stop working once Pages is re-enabled).
