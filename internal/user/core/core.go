// Package core is the domain core of the User Directory & Auth hexagon
// (AD-2). It will own the business rules for users, groups, permissions,
// sessions, credentials and recovery flows. Cold-start Story 1.1 ships only
// the package boundary; the domain shapes arrive with stories 1.3-1.10.
package core
